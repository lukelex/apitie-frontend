import { radioButton } from '../../../helpers/radio-button';
import { module, test } from 'qunit';

module('Unit | Helper | radio button');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = radioButton(42);
  assert.ok(result);
});
